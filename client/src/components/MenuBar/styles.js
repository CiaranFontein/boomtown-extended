const styles = theme => ({
  toolbar: {
    position: "absolute",
    display: "flex",
    justifyContent: "space-between"
  },
  rightSideIcons: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center"
  },
  flexToolbar: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    height: 64
  },
  iconButton: {
    fontSize: 13
  }
});

export default styles;
