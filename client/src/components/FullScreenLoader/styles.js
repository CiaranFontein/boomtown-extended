const styles = theme => ({
  container: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#212121",
    position: "relative",
    height: "100vh"
  }
});
export default styles;
