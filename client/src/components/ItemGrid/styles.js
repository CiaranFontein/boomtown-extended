const styles = theme => ({
  itemGrid: {
    display: "flex",
    flexWrap: "wrap",
    minHeight: "100vh",
    justifyContent: "center"
  },
  icon: {
    color: "rgba(255, 255, 255, 0.54)"
  },
  noItemsMessage: {
    color: "#F5A841",
    fontSize: 64
  }
});

export default styles;
