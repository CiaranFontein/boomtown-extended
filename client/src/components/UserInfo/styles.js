const styles = theme => ({
  userShortInfo: {
    margin: theme.spacing(1),
    display: "flex",
    justifyContent: "spaceBetween",
    alignItems: "center",
    textDecoration: "none",
    color: "black"
  },
  userInfo: {
    margin: theme.spacing(1),
    borderRadius: "50%",
    textDecoration: "none",
    color: "black"
  }
});
export default styles;
