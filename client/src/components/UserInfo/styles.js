const styles = theme => ({
  userShortInfo: {
    margin: theme.spacing(1),
    display: "flex",
    justifyContent: "spaceBetween",
    alignItems: "center"
  },
  userInfo: {
    margin: theme.spacing(1),
    borderRadius: "50%"
  }
});
export default styles;
