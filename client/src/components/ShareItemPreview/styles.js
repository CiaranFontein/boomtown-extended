const styles = theme => ({
  previewCard: {
    height: "auto",
    width: 400
  },
  borrowButton: {
    textTransform: "uppercase",
    margin: theme.spacing(3)
  },
  userInfo: {
    margin: theme.spacing(1.5)
  },
  itemTitle: {
    margin: theme.spacing(1.5)
  },
  itemDescription: {
    margin: theme.spacing(1.5)
  },
  panel: {
    margin: theme.spacing(3)
  }
});

export default styles;
