const styles = theme => ({
  previewCard: {
    height: "auto",
    width: 400
  },
  borrowButton: {
    textTransform: "uppercase"
  },
  userInfo: {
    margin: theme.spacing(1.5)
  },
  itemTitle: {
    margin: theme.spacing(1.5)
  }
});

export default styles;
